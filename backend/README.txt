Backend Ofertuś (Python + FastAPI)

1. cd backend
2. pip install -r requirements.txt
3. uvicorn server:app --reload --host 0.0.0.0 --port 8000

Frontend łączy się z http://localhost:8000/generate-offer
